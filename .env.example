# Database bootstrap
# Copy this file to .env and change every value locally – the docker-compose
# stack will refuse to start otherwise.
POSTGRES_DB=examapp
POSTGRES_USER=examapp
POSTGRES_PASSWORD=examapp
